<!--components/playList/index.wxml-->
<view class="container" bind:tap="goToMusicList">
    <image src="{{playList.picUrl}}" class="img"></image>
    <text class="count">{{_count}}</text>
    <text class="name">{{playList.name}}</text>
</view>